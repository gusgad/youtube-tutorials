services:
  redis:
    image: redis/redis-stack:latest
    ports:
      - "6379:6379"
      - "8001:8001"   # RedisInsight UI — useful for the demo
    volumes:
      - redis-data:/data

volumes:
  redis-data:
